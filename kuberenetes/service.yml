apiVersion: v1
kind: Service
metadata:
  name: udacity-capstone-project-service

spec:
  selector:
    app: udacity-capstone-project
    version: green
  type: LoadBalancer    
  ports:
    - protocol: TCP  
    - port: 8000
      targetPort: 8000
